{
  "name": "momence",
  "description": "Task for Momence",
  "scripts": {
    "web:dev": "npm run start --workspace web",
    "server:build": "npm run build --workspace server",
    "common:build": "npm run build --workspace common"
  },
  "workspaces": [
    "common",
    "server",
    "web"
  ],
  "dependencies": {
    "typescript": "^4.9.5"
  }
}
